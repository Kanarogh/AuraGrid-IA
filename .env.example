# Provedor ativo: gemini | groq | openrouter | ollama
AI_PROVIDER=gemini

# Só o provedor/modelo escolhido no painel (sem trocar para Groq/Gemini/outro modelo OpenRouter).
# AI_ALLOW_FALLBACK=1

# --- Ollama (Gemma 4 local, sem API key) ---
# Requer Ollama rodando: https://ollama.com — depois: ollama pull gemma4:e4b
# OLLAMA_BASE_URL="http://127.0.0.1:11434"
# OLLAMA_MODEL="gemma4:e4b"
# OLLAMA_TIMEOUT_MS=180000
# OLLAMA_CLOUD_FALLBACK=1   # se Ollama falhar, tenta Gemini/Groq/OpenRouter
# OLLAMA_DISABLED=1   # esconde Ollama no painel de IA

# --- Google Gemini ---
GEMINI_API_KEY="MY_GEMINI_API_KEY"
# GEMINI_MODEL="gemini-2.0-flash"

# --- Groq (Llama 4 Scout com visão) ---
GROQ_API_KEY="MY_GROQ_API_KEY"
# GROQ_MODEL="meta-llama/llama-4-scout-17b-16e-instruct"

# --- OpenRouter (fallback para visão, free tier) ---
# Crie em https://openrouter.ai/keys (gratuito). Modelos free com visão:
#   google/gemma-4-31b-it:free                        (visão, recomendado — catálogo/legendas)
#   google/gemma-4-26b-a4b-it:free                    (visão MoE, texto+imagem+vídeo curto)
#   qwen/qwen2.5-vl-32b-instruct:free                 (visão)
#   openrouter/free                                 (roteador; último recurso no catálogo)
#   minimax/minimax-m2.5:free                       (texto, grátis, ótimo em legendas)
#   minimax/minimax-m2.7                            (texto, pago)
OPENROUTER_API_KEY=""
# OPENROUTER_MODEL="openrouter/free"

APP_URL="http://localhost:3000"

# Logs detalhados de IA no terminal (tentativas, tipo de falha, corpo parcial)
# AI_DEBUG=1
